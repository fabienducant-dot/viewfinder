"use strict";
const {getStore}=require("@netlify/blobs");const crypto=require("crypto");
const {composeBrandPoster}=require("./_shared/brand-compositor");const {chooseLayout,analyzeActualImage,normalizePlatform}=require("./_shared/v3-layout-engine");const {compatibilityMatrix,cropPosition}=require("./_shared/v3-campaign");const {assessQuality}=require("./_shared/v3-quality");const {buildCostAudit}=require("./_shared/v3-cost-control");
function store(name){const opts={consistency:"strong"};if(process.env.BLOBS_SITE_ID&&process.env.BLOBS_TOKEN)return getStore({name,siteID:process.env.BLOBS_SITE_ID,token:process.env.BLOBS_TOKEN,...opts});return getStore({name,...opts});}
async function rawBuffer(record){if(record.b64)return Buffer.from(record.b64,"base64");if(!record.url)throw new Error("Photographie brute indisponible.");const res=await fetch(record.url);if(!res.ok)throw new Error("Photographie brute distante inaccessible.");return Buffer.from(await res.arrayBuffer());}
exports.handler=async event=>{if(event.httpMethod!=="POST")return {statusCode:405,body:"Method Not Allowed"};try{const body=JSON.parse(event.body||"{}");if(!body.sourceJobId||!body.platform)return {statusCode:400,body:JSON.stringify({error:"sourceJobId et platform requis"})};const jobs=store("viewfinder-image-jobs"),brand=store("viewfinder-data");const statusRaw=await jobs.get(`jobs/${body.sourceJobId}`);if(!statusRaw)return {statusCode:404,body:JSON.stringify({error:"Job source introuvable"})};const source=JSON.parse(statusRaw);if(!source.rawResultKey||!source.v3Plan||!source.v3Finalization?.analysis)return {statusCode:409,body:JSON.stringify({error:"Raw-result ou analyse V3 indisponible"})};const matrix=compatibilityMatrix(source.v3Finalization.analysis);const platform=normalizePlatform(body.platform);const compatibility=matrix[platform];if(!compatibility?.platformAdaptationPossible)return {statusCode:409,body:JSON.stringify({error:compatibility?.reason||"Format incompatible",platformAdaptationPossible:false,estimatedAdditionalPhotoEur:body.estimatedAdditionalPhotoEur||null,requiresExplicitConfirmation:true})};const [rawRecord,logoRecord]=await Promise.all([jobs.get(source.rawResultKey),brand.get("vf-logo-asset")]);if(!rawRecord||!logoRecord)throw new Error("Photographie brute ou logo officiel absent.");const image=await rawBuffer(JSON.parse(rawRecord));const logo=JSON.parse(logoRecord);const analysis=analyzeActualImage(source.v3Finalization.analysis);const layout=chooseLayout({platform,contract:source.v3Plan.contract,analysis});layout.cropPosition=cropPosition(analysis);const final=await composeBrandPoster({imageBuffer:image,logoDataUrl:logo.dataUrl,platform,headline:String(body.headline||""),zoneText:String(body.zoneText||""),selectedLayout:layout,posterStrategy:source.v3Plan.posterStrategy});const quality=assessQuality({contract:source.v3Plan.contract,analysis,composition:{imageExists:true,logoIntegrity:true,marginsValid:true,textWithinCanvas:true,protectedCollision:false,logoRectangleOpaque:false,contrastValid:true,gazeHierarchyValid:true,thumbnailImpact:true}});const derivedJobId=crypto.randomUUID(),resultKey=`jobs/${derivedJobId}/result`;await jobs.set(resultKey,JSON.stringify({b64:final.toString("base64"),url:null,brandComposited:true,recomposedFrom:body.sourceJobId}));await jobs.set(`jobs/${derivedJobId}`,JSON.stringify({jobId:derivedJobId,status:"completed",createdAt:Date.now(),updatedAt:Date.now(),resultKey,rawResultKey:source.rawResultKey,v3Plan:source.v3Plan,v3Finalization:{analysis,layout,quality},recomposedFrom:body.sourceJobId,costAudit:buildCostAudit({mode:"recompose",visionUsage:false,imageCalls:0})}));const versions=[...(source.derivedVersions||[]),derivedJobId];await jobs.set(`jobs/${body.sourceJobId}`,JSON.stringify({...source,derivedVersions:versions,updatedAt:Date.now()}));return {statusCode:200,headers:{"Content-Type":"application/json"},body:JSON.stringify({ok:true,jobId:derivedJobId,resultUrl:`/.netlify/functions/get-image-result?jobId=${derivedJobId}`,platform,layout,quality,costAudit:buildCostAudit({mode:"recompose",visionUsage:false,imageCalls:0}),imageGenerationCalls:0})};}catch(error){return {statusCode:500,headers:{"Content-Type":"application/json"},body:JSON.stringify({error:String(error.message||error),imageGenerationCalls:0})};}};
exports.rawBuffer=rawBuffer;
