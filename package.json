{
  "name": "viewfinder",
  "private": true,
  "scripts": {
    "build:logo": "node scripts/build-sdz-logo-asset.js",
    "verify:index": "node scripts/verify-index-integrity.js",
    "test": "node --test tests/*.test.js",
    "verify": "npm run build:logo && npm run verify:index && npm test"
  },
  "dependencies": {
    "@fontsource/cormorant-garamond": "^5.3.0",
    "@fontsource/manrope": "^5.3.0",
    "@netlify/blobs": "^7.0.0",
    "opentype.js": "^1.3.4",
    "sharp": "0.35.4"
  },
  "devDependencies": {
    "jsdom": "^26.1.0"
  }
}
